# Calibration scripts

Calibration entry points belong here. Each script must use a main guard, expose
all machine-specific values as arguments or configuration, require explicit
hardware confirmation, and restore safe device state on failure.
